import { Route } from '@angular/router';
import { CurrentBookingComponent } from './index';

export const CurrentBookingRoutes: Route[] = [
  	{
    	path: 'currentbooking',
    	component: CurrentBookingComponent
  	}
];
