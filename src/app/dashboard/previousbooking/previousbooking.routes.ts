import { Route } from '@angular/router';
import { PreviousBookingComponent } from './index';

export const PreviousBookingRoutes: Route[] = [
  	{
    	path: 'previousbooking',
    	component: PreviousBookingComponent
  	}
];
